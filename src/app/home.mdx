export const meta = {
  author: 'Kayden VG',
  title: 'Home - Test Blog',
  slug: 'home',
  topics: [
    'technical writing',
    'software engineering',
    'technical writing basics',
  ],
};

# Test Blog

A static template for GitHub Pages from Next.js with MDX. Edit `src/app/home.mdx`.

---

<div style={{ display: 'flex', justifyContent: 'center' }}>
  <img src="/camera.png" alt="Camera" style={{ width: '48px', height: '48px' }} />
</div>

export default ({ children }) => <div className="prose">{children}</div>;